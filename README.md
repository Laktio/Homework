# Проект "Виджет банковских операций клиента"
## Описание
Проект "Виджет банковских операций клиента" это приложение для визуализации 
банковских операций. Приложение принимает на вход номера счетов и банковских карт
и выводит их замаскированные версии. Приложение принимает информацию по 
банковским операциям и сортирует их по требуемым параметрам. Приложение принимает 
информацию по транзакциям и выводит поэтапно транзакции с соответствующим типом валюты 
операции. Приложение принимает список транзакций в формате JSON файла и преобразует в список словарей. 
Далее рассматривает отельно транзакции и выводят стоимость в рублях и конвертирует в рубли если валюта
другая (с применением API c https://apilayer.com)

## Установка
1. клонируйте репозиторий
`git@github.com:Laktio/Homework.git`
или
`https://github.com/Laktio/Homework.git`

## Использование
1. подавать на вход номера счетов и карт и получать замаскированные версии
2. подавать на вход информацию по операциям по клиентам и получать отсортированные списки данных
3. подавать на вход информацию по транзакциям и получать поэтапно транзакции с соответствующим 
типом валюты операции
4. подавать на вход информацию по транзакциям и получать поэтапно описание операции
5. реализован декоратор для логирования корректности работы функций в файл melog.txt если 
указан в параметрах декоратора или в консоль если не указан
6. реализована функция преобразующая список из файла в формате JSON в формат Python
7. реализована функция выводящая стоимость из транзакций с конвертированием в рубли (с 
применением API c https://apilayer.com)
8. реализована функция по преобразованию данных файла CSV в список словарей
9. реализована функция по преобразованию данных файла EXCEL в список словарей
10. реализованы функции для фильтрации списков словарей
11. реализована функция main для взаимодействия с пользователем и вывода списка операций по запросу

 
Проект не завершен

## Тестирование

1. Тестирование функций из модуля masks.py осуществляется в модуле test_masks.py
2. Тестирование функций из модуля widget.py осуществляется в модуле test_widget.py
3. Тестирование функций из модуля processing.py осуществляется в модуле test_processing.py
4. Тестирование функций из модуля generators.py осуществляется в модуле generators.py
5. Тестирование вызова ошибок декоратора при некорректной работе функции
6. Тестирование вывода сообщений на консоль декоратора, если не указан файл для сохранения
логов
7. тестирование генераторов номеров карт и счетов
8. тестирование функций вывода списка транзакций из формата JSON в формат Python
9. тестирование функций вывода стоимости с конвертированием валюты в рубли
10. тестирование функций по преобразованию данных из файлов CSV и EXCEL в список словарей
11. тестирование функций по фильтрации списков словарей

Покрытие тестами составляет: **91%**

## Докуемнтация
Пока ничего (расчитываю на объяснения в этом вопросе)

## Лицензии
Пока ничего (расчитываю на объяснения в этом вопросе)





